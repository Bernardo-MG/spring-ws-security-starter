put:
  summary: Change the password for the current session user.
  operationId: changePassword
  tags: [PasswordChange]
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../components/password_change.yml#/components/schemas/PasswordChange'
  responses:
    '204':
      description: Password successfully changed. No content is returned.