post:
  summary: Start a password reset process.
  operationId: requestPasswordReset
  tags: [PasswordResetRequest]
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../components/password_reset.yml#/components/schemas/RequestPasswordReset'
  responses:
    '200':
      description: Password reset process started.