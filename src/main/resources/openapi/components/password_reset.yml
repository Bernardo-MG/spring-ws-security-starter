components:
  schemas:
    PasswordReset:
      type: object
      required: [password]
      properties:
        password:
          type: string
          format: password
          description: New password to set.