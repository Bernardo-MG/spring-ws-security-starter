post:
  summary: Create a user.
  operationId: inviteUser
  tags: [UserOnboarding]
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../components/users.yml#/components/schemas/UserCreation'
  responses:
    '201':
      description: Invited, and created, user.
      content:
        application/json:
          schema:
            $ref: '../components/users.yml#/components/schemas/UserResponse'