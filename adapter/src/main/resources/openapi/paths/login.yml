post:
  summary: Handle login.
  operationId: login
  tags: [Login]
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../components/login.yml#/components/schemas/LoginRequest'
  responses:
    '200':
      description: Login status.
      content:
        application/json:
          schema:
            $ref: '../components/login.yml#/components/schemas/TokenLoginStatusResponse'